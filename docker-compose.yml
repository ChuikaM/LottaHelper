version: '3.8'

services:
  ai-webhook:
    build: .
    ports:
      - "8000:8000"
    expose:
      - 8000
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - TARGET_URL=${TARGET_URL}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped